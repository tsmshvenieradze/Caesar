## Summary

<!-- What changed and why. -->

## Checklist

- [ ] `dotnet test Caesar.slnx` passes locally
- [ ] Public API changes are documented in README.md
- [ ] `<VersionPrefix>` in `Directory.Build.props` is bumped if this should ship to nuget.org on merge
